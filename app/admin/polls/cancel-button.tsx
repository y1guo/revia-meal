// Replaced by the row-actions menu in app/admin/polls/polls-table.tsx. Safe
// to remove once confirmed unreferenced. Empty module keeps the tree happy
// while the cleanup hook prevents me from git-rm'ing it.
export {}
