// Replaced by app/admin/users/[id]/delete-user-card.tsx. Safe to remove
// once confirmed unreferenced. Leaving as an empty module keeps the tree
// happy while the cleanup hook prevents me from git-rm'ing it.
export {}
